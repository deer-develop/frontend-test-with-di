import React from "react";

const ProductTour = (): JSX.Element => {
  return (
    <div className={"flex h-screen items-center justify-center"}>
      <div>회원 가입 페이지</div>
    </div>
  );
};

export default ProductTour;
